# treinamentoCalabash
```
git pull - Traz o conteúdo de uma branch A para a branch
git pull origin master conteúdo da master (origin) para a local
git clone - clona o repositório

git checkout -b "nome-branch" - cria uma branch local

git status

git add +arquivo - adiciona arquivo para subir para o repositório remoto

git commit -m "mensagem" - commita

git push - empurra para o repositório

git reset HEAD README.MD
```
